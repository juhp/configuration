#!/bin/sh

BUILD=no

if [ "$BUILD" = "yes" ]; then
#    pushd ~/haskell/stackage-curator
#    git pull
   stack-nightly install stackage-curator
#    popd
#:
fi

stack update
stack --resolver lts-8 exec /home/petersen/.local/bin/stackage-curator check
