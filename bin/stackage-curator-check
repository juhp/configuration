#!/bin/sh

CURATOR=curator

RESOLVER=lts-14

pushd ~/haskell/$CURATOR
git pull
stack --resolver $RESOLVER install $CURATOR
popd

#stack update
#stack --resolver $RESOLVER exec -- $STACKAGE_CURATOR constraints --target nightly-$(date -u +%F)
#stack --resolver $RESOLVER exec -- $STACKAGE_CURATOR check-snapshot
